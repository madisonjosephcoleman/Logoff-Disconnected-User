# Logoff Disconnected User
This powershell script, when ran will logoff disconnected users. You can set this to run automatically in task scheduler with the trigger is "on disconnect from user session".
